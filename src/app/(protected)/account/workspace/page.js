import { ProjectAccountWorkspace } from "@/domains/project/account-extension";

export const metadata = { title: "Project area" };

export default function WorkspacePage() {
  return <ProjectAccountWorkspace />;
}
