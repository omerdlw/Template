import { REGISTRY_KEYS } from "@/modules/registry";

export const CURRENT_PAGE_KEY = REGISTRY_KEYS.CONTEXT_MENU_CURRENT;
export const GLOBAL_MENU_KEY = "*";
export const MENU_SCREEN_MARGIN = 10;

export const CONTEXT_MENU_LAYOUT = Object.freeze({
  wrapperRadius: 24,
  wrapperPadding: 10,
});
